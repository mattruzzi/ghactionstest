# This is a basic workflow that is manually triggered

name: Linux

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  push:
    branches: [master]
    paths: .github/workflows/linux.yml
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: "Person to greet"
        # Default value if no value is explicitly provided
        default: "World"
        # Input has to be provided for the workflow to run
        required: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: install scrot
        run: sudo apt install scrot
      - uses: actions/checkout@v2
      - name: Send greeting
        run: echo "Hello ${{ github.event.inputs.name }}" && ls -a && pwd && ls && unzip nativefier-gui-linux-x64.zip && ./nativefier-gui-linux-x64/nativefier-gui & sleep 25 && scrot test.jpg && pwd && ls
      - name: Upload screenshot
        uses: actions/upload-artifact@v2
        with:
          name: test.jpg
          path: |
            /Users/runner/work/ghactionstest/ghactionstest/*.jpg
            /Users/runner/work/ghactionstest/ghactionstest/*.png
